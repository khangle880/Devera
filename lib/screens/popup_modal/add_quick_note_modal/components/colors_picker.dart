import 'package:flutter/material.dart';

class ColorsPicker extends StatelessWidget {
  const ColorsPicker({Key? key}) : super(key: key);
  //TODO: Im too lazy for this :D
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
